---
name: git-rebase-with-ledger
description: Deprecated name for ledger-rebase — the skill was renamed. Loads when the user names the old skill; delegates to ledger-rebase, which runs the workflow — Ledger-first rebase: conflict ledgers on disk, verify, fallout commit; ask-gate before any MR comment.
disable-model-invocation: true
---

Call the Skill tool with "ledger-rebase".